# Setup & Configure
docker run -d --name Pihole -e ServerIP=192.168.103.73 -e TZ=America/New_York -e WEBPASSWORD=Password -e DNS1=8.8.8.8 -e DNS2=1.1.1.1 -p 8008:80 -p 53:53/tcp -p 53:53/udp -p 443:443 -v ~/Files/Pihole/:/etc/pihole/ --dns=127.0.0.1 --dns=8.8.8.8 --cap-add=NET_ADMIN --restart=unless-stopped pihole/pihole:latest

# Note: 
MacOS docker installations, need to edit the file "~/Library/Group/Containers/group.com.docker/settings.json" > Change "kernelForUDP": true" to "kernelForUDP": false"
MacOS v14.x uses port 53 by default, and the change above resolves the container not running.