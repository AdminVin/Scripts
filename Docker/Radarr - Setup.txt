# Install & Configure

#> Windows: Can be run from a CMD/Terminal Prompt
#> Synology: Run as scheduled Task (as root) / SSH > "sudo -i" (without quotes)

# Synology
docker run -d \
  --name=Radarr \
  -e PUID=1026 \
  -e PGID=100 \
  -e TZ=US/Eastern \
  -p 7878:7878 \
  -v /volume1/docker/Radarr:/config \
  -v /volume1/Media/Movies:/movies \
  -v /volume2/SABnzbd/complete:/downloads \
  --restart unless-stopped \
  lscr.io/linuxserver/radarr:latest